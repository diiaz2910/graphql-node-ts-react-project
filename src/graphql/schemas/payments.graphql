type Query {
  getPayments: [Payment]
}

type Mutation {
  createPayments(payment: newPaymentInput): String
}

type Payment {
  _id: ID
  amount: String
  description: String
}

input newPaymentInput {
  amount: String
  description: String
}
